Saurav


git config user.name "pragya"

git config user.email "pragya@cse.iitb.ac.in"

git config core.editor emacs

git config merge.tool meld

git config --list

git reflog

git checkout 2b71b5d main.sh binaries/dep1 binaries/dep2 binaries/dep3 binaries/dep4 binaries/dep5

bash main.sh 

git add .

git commit -m "Everything is fine again"

git reflog -p

git show 2415f77

rm -r *

git add .

git commit -m "deleted all files"

git checkout 8e9c742 main.sh binaries/dep1 binaries/dep2 binaries/dep3 binaries/dep4 binaries/dep5 misc/file1.txt misc/file2.txt

git commit -m "Everything is fine again"

git rebase -i HEAD~4

git add main.sh

git rebase --continue

git add .

git commit -m "culprit info added"