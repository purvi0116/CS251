Saurav

Commands:

git config user.name "pragya"

git config user.email "pragya@cse.iitb.ac.in"

git config core.editor emacs

git config merge.tool meld

git config --list

git reflog

git checkout 2b71b5d main.sh binaries/dep1 binaries/dep2 binaries/dep3 binaries/dep4 binaries/dep5

bash main.sh

git commit -m "Everything is fine again"

git reflog -p

git show 2415f77

rm -r *

git commit -m "deleted all files"

git add main.sh binaries/dep1 binaries/dep2 binaries/dep3 binaries/dep4 binaries/dep5 misc/file1.txt misc/file2.txt

git commit -m "deleted all files"

 git checkout eee93e8 main.sh binaries/dep1 binaries/dep2 binaries/dep3 binaries/dep4 binaries/dep5 misc/file1.txt misc/file2.txt

git commit -m "Everything is fine again"

git rebase -i HEAD~4

git add main.sh

git rebase --continue

bash main.sh

git reflog

git log
